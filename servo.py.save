import RPi.GPIO as g
import time, random

pin = 40

g.setmode(g.BCM)
g.setup(pin, g.OUT)

def delayMS(x):
	time.sleep(x/1000)

count = 0
while True:
	if (count%50==0):
		delayLeng
th = random.randint(1,30)/2
	g.output(pin, True)
	delayMS(delayLength)
	g.output(pin, False)
	delayMS(20-delayLength)
	count+=1
	
